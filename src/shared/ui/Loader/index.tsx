import {CirclesWithBar} from 'react-loader-spinner'

export const Loader = () => {
    return (
        <CirclesWithBar
            height="80"
            width="80"
            color="#3498db"
            ariaLabel="audio-loading"
        />
    );
};